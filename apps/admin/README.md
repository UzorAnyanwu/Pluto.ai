# apps/admin (placeholder — pending decision)

Per [`docs/architecture/01-system-architecture.md`](../../docs/architecture/01-system-architecture.md)
§8, the current recommendation is **one** Next.js app (`apps/web`) with a `(platform-admin)` route
group, not a separate app here. This directory is kept as a placeholder in case that decision is
overridden on review; if the recommendation stands, this directory is removed (not built out) when
Phase 2 frontend work starts.
