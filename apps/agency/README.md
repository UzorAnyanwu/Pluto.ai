# apps/agency (placeholder — pending decision)

Same status as [`apps/admin`](../admin/README.md): current recommendation is a `(agency)` route
group inside `apps/web`, not a standalone app. Kept as a placeholder pending architecture review.
