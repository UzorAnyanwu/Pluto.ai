# packages/shared-types

TypeScript types shared across `apps/web`, generated from `services/api-core`'s OpenAPI schema so
frontend and backend contracts never drift silently. Not yet implemented — lands alongside the
first `api-core` endpoints in Phase 2.
