# packages/config

Shared ESLint, TypeScript, and Tailwind configuration used across `apps/` and `packages/` so linting
and build rules stay consistent instead of drifting per-app. Not yet implemented — lands in Phase 2.
